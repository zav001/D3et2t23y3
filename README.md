# Gooba Client — screenshot-matched layout

This version updates the homepage layout and styling to closely match the supplied purple/black Gooba Client reference image.

Files:
- `index.html` — page structure
- `styles.css` — responsive styling and client UI mockup
- `script.js` — config, feature cards, and FAQ behavior

The hero/client preview is recreated in HTML/CSS rather than embedding the reference screenshot.
